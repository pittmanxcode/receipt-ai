__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# Sync ledger and cached Keep session. Machine-local: the ledger records what
# this checkout last agreed with Keep, and the session cache holds auth state.
.keep-bridge/
